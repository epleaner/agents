# OpenCode Agents - Environment Variables
# Put your actual keys in .envrc.local (gitignored), then run: direnv allow

# Load local overrides if present
if [[ -f .envrc.local ]]; then
  source .envrc.local
fi
