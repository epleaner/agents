## 1. Spec Split
- [x] 1.1 Create `openspec/specs/meta-learnings/spec.md` capturing the ledger/index requirements, review command workflow, and promotion lifecycle currently embedded in `codex-multi-agent-suite`.
- [x] 1.2 Reference the Meta-Agent’s enforcement responsibilities within the new spec so ownership is clear.

## 2. Codex Spec Update
- [x] 2.1 Replace the inline Meta-Learnings requirement in `codex-multi-agent-suite` with a concise reference pointing to the new capability.
- [x] 2.2 Ensure the spec still notes the Meta-Agent’s responsibility to maintain the ledger workflow.

## 3. Validation
- [x] 3.1 Run `openspec validate add-meta-learnings-spec --strict` and resolve any issues.
