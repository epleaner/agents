## Spec Promotion Checklist
- [x] Migrate Portable OpenCode Configuration Bundle spec to `openspec/specs/portable-config-bundle/` and seed its `tasks.md`.
- [x] Migrate GPT-5.1 Codex Multi-Agent Suite spec to `openspec/specs/codex-multi-agent-suite/` and seed its `tasks.md`.
- [x] Migrate Knowledge Graph Platform spec to `openspec/specs/knowledge-graph-platform/` and seed its `tasks.md`.
- [x] Migrate External Workflow Integration (Slack/Jira/Linear/Fathom) spec to `openspec/specs/external-workflow-integration/` and seed its `tasks.md`.
- [x] Migrate Action Item Management spec to `openspec/specs/action-item-management/` and seed its `tasks.md`.
- [x] Migrate Cloud Deployment Workflow spec to `openspec/specs/cloud-deployment-workflow/` and seed its `tasks.md`.

## Follow-up Guidance
- [ ] For each capability, keep its dedicated `tasks.md` updated as work progresses (reference those files directly going forward).
- [ ] Use `openspec/specs/.../tasks.md` as the source of truth when scoping future beads issues related to that capability.
