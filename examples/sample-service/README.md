# Sample Service

This directory acts as a sandbox repository for testing the portable OpenCode configuration bundle.

- `.opencode/shared/` is populated by `bin/portable-config-bundle update --repo examples/sample-service`.
- `.opencode/local/` contains overrides that belong solely to this service; edit files here to customize agent instructions or add service-specific skills.
