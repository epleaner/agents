# Local Overrides

Place any service-specific AGENTS instructions, skill configs, or plugins here. Files in this directory are never overwritten by `bin/portable-config-bundle update`, so you can safely track custom commands, credentials scaffolding, or onboarding notes unique to this service.
